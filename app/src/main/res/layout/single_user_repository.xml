<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="18dp"
    android:orientation="vertical"
    android:background="@drawable/single_user_repository_background">

    <!-- Repository title -->
    <TextView
        android:id="@+id/repoTitle"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:textSize="22sp"
        android:textColor="@color/blue"
        android:textStyle="bold"/>

    <!-- Repository description -->
    <TextView
        android:id="@+id/repoDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:textColor="@color/grey"
        android:textSize="17sp"/>

    <!-- Repository language, stars, forks and updated at -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="horizontal">

        <!-- Language -->
        <TextView
            android:id="@+id/repoLanguage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textColor="@color/grey"
            android:textSize="17dp"
            android:layout_marginRight="10dp"/>

        <!-- Stars -->
        <TextView
            android:id="@+id/repoStars"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="10dp"
            android:textColor="@color/grey"
            android:drawableLeft="@drawable/ic_star"/>

        <!-- Forks -->
        <TextView
            android:id="@+id/repoForks"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="10dp"
            android:textColor="@color/grey"
            android:drawableLeft="@drawable/ic_fork"/>

        <!-- When updated -->
        <TextView
            android:id="@+id/repoWhenUpdated"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/grey"
            android:textSize="15dp"/>

    </LinearLayout>

</LinearLayout>